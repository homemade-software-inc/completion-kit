<section class="ck-page-header">
  <div>
    <p class="ck-step">Setup</p>
    <h1 class="ck-title">New metric</h1>
    <p class="ck-lead">Define one method for assessing outputs.</p>
  </div>
  <div class="ck-actions">
    <%= link_to "All metrics", metrics_path, class: ck_button_classes(:light, variant: :outline) %>
  </div>
</section>

<%= render "form", metric: @metric %>
