<%= render "completion_kit/shared/settings_nav", section_label: "Tags" %>

<section class="ck-page-header">
  <div>
    <h1 class="ck-title">Tags</h1>
    <p class="ck-lead">Domain labels for organizing metrics, prompts, runs, and datasets.</p>
  </div>
  <div class="ck-actions">
    <%= link_to "New tag", new_tag_path, class: ck_button_classes(:dark) %>
  </div>
</section>

<% if @tags.any? %>
  <div class="ck-tags-list">
    <% @tags.each do |tag| %>
      <% count = @tagging_counts.fetch(tag.id, 0) %>
      <div class="ck-tags-list__row" style="--row-color: var(--tag-<%= tag.color %>);">
        <div class="ck-tags-list__edge"></div>
        <div class="ck-tags-list__name"><%= tag.name %></div>
        <div class="ck-tags-list__count <%= "ck-tags-list__count--zero" if count.zero? %>">
          <%= count %> <%= "item".pluralize(count) %>
        </div>
        <div class="ck-tags-list__actions">
          <%= link_to "Edit", edit_tag_path(tag), class: "ck-link" %>
          <%= button_to "Delete", tag_path(tag), method: :delete,
                form: { data: { turbo_confirm: "Delete \"#{tag.name}\"? This removes the tag from every linked metric, prompt, run, and dataset." } },
                class: "ck-link" %>
        </div>
      </div>
    <% end %>
  </div>
<% else %>
  <div class="ck-empty">
    <p>No tags yet. <%= link_to "Create your first tag", new_tag_path, class: "ck-link" %> to start organizing.</p>
  </div>
<% end %>
